const ReviewOrderStage = ({ setStage }) => {
    return (
        <>
            <h2>stage 3</h2>
            <button onClick={() => setStage(2)}>Back</button>
        </>
    );
};

export default ReviewOrderStage;